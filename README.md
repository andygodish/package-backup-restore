# Package: Backup Restore

Deploy the [Core Backup Restore](https://github.com/defenseunicorns/uds-core) package configured to your environment.

- ~35s pod startup time with default package configuration
